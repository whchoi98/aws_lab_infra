apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: orders
  labels:
    app: orders
spec:
  selector:
    app: orders
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
